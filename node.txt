ejs setup for this step we need
1) ejs install: -- npm i ejs
2) configure ejs -- app.set("view engine", "ejs")
3) make a 1 folder of view
4) make ejs file in view folder
5)take render with place ot send --> when we render any file to make sure take on file which is 
    in view folder not from another folder  and do not mention .ejs in render function


static files setup krne k liye

1) create a folder called public

2) create three folder inside it, images,stylesheets, jaaascript

3) configure the express static in index.js

4)